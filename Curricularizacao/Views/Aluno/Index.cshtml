@model IEnumerable<Curricularizacao.Models.Aluno>
@{
    ViewData["Title"] = "Alunos";
}

<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Telefone</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var aluno in Model)
        {
            <tr>
                <td>@aluno.Nome</td>
                <td>@aluno.Telefone</td>
                <td>
                    <a asp-action="Details" asp-route-id="@aluno.Id" class="btn btn-info">Detalhes</a> | 
                    <a asp-action="Edit" asp-route-id="@aluno.Id" class="btn btn-warning">Editar</a> | 
                    <a asp-action="Delete" asp-route-id="@aluno.Id" class="btn btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" class="btn btn-primary">Adicionar Aluno</a>